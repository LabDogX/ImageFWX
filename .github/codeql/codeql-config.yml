name: "CodeQL Config"

paths-ignore:
  - "**/*.md"
  - "docs/**"

queries:
  - uses: security-and-quality

query-filters:
  - exclude:
      id: py/path-injection
      problem.severity: recommendation
